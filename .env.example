# Cosmos Atlas needs no environment variables to build or run.
# This file is kept as a placeholder in case you wire in a live data source
# later (e.g. a NASA API key for supplementary imagery). Never commit a real
# key — add it in your Cloudflare Pages project settings instead.

# NASA_API_KEY=
