# كل المتغيرات هنا اختيارية — الموقع يعمل بالكامل ببيانات محلية موثقة بدونها.
# All variables are optional — the site works fully on local verified data without them.

# Only needed if/when you wire in NASA's public APIs for supplementary,
# clearly-labeled "live data" (e.g. APOD, Image and Video Library, Exoplanet
# Archive). Get a free key at https://api.nasa.gov — never commit a real key.
NASA_API_KEY=
