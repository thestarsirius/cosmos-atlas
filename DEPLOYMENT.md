# النشر المجاني — من الصفر إلى موقع منشور

لا حاجة لشراء أي شيء: لا نطاق، ولا استضافة، ولا واجهات برمجية مدفوعة. سيصبح موقعك متاحًا على رابط مجاني
من نوع `https://cosmos-atlas.vercel.app`.

## الخطوة 1 — إنشاء حساب GitHub

اذهب إلى github.com وأنشئ حسابًا مجانيًا إن لم يكن لديك واحد.

## الخطوة 2 — رفع المشروع إلى GitHub

من داخل مجلد المشروع على جهازك:

```bash
git init
git add .
git commit -m "أطلس الكون: البنية الأساسية"
```

على GitHub: أنشئ مستودعًا جديدًا فارغًا (بدون README أو .gitignore، لأنهما موجودان بالفعل هنا)، ثم:

```bash
git remote add origin https://github.com/USERNAME/cosmos-atlas.git
git branch -M main
git push -u origin main
```

استبدل `USERNAME` باسم حسابك، و`cosmos-atlas` باسم المستودع الذي اخترته.

## الخطوة 3 — إنشاء حساب Vercel مجاني

اذهب إلى vercel.com واختر "تسجيل الدخول عبر GitHub" (Sign up with GitHub). هذا يربط الحسابين تلقائيًا،
والخطة المجانية (Hobby) كافية تمامًا لهذا المشروع.

## الخطوة 4 — استيراد المستودع

من لوحة تحكم Vercel: **Add New → Project**، ثم اختر مستودع `cosmos-atlas` من القائمة واضغط **Import**.

## الخطوة 5 — إعدادات المشروع

Vercel يكتشف أن المشروع Next.js تلقائيًا ولا يحتاج أي تعديل في إعدادات البناء (Build Command / Output
Directory تُملأ تلقائيًا).

## الخطوة 6 — متغيرات البيئة (اختياري)

المشروع يعمل بالكامل بدون أي متغير بيئة. إذا أردت لاحقًا ربط واجهة NASA البرمجية المجانية، أضف
`NASA_API_KEY` في تبويب **Environment Variables** — راجع `.env.example`.

## الخطوة 7 — النشر

اضغط **Deploy**. تستغرق العملية عادة دقيقة إلى دقيقتين.

## الخطوة 8 — فتح الموقع

بعد اكتمال النشر، يعطيك Vercel رابطًا مباشرًا من نوع `https://cosmos-atlas-username.vercel.app`. هذا هو
موقعك المنشور فعليًا ومجانًا بالكامل.

## الخطوة 9 — التحديثات المستقبلية

من الآن فصاعدًا، أي تعديل تدفعه (`git push`) إلى فرع `main` على GitHub سيُنشر تلقائيًا على نفس الرابط خلال
دقائق — لا حاجة لإعادة الاستيراد أو أي خطوة يدوية إضافية.

## نطاق مخصص (اختياري تمامًا)

الرابط المجاني من Vercel كافٍ تمامًا للنشر العام ولا يتطلب أي خطوة إضافية. ربط نطاق مخصص لاحقًا ممكن لكنه
غير ضروري ولن نطلبه هنا.
